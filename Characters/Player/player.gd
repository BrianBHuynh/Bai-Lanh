extends Area2D

@export var health = 100 #health amount of player
@export var attack = 20 #attack value of the player
@export var defense = 20 #defense of the player
@export var pref_pos = "None" #prefered possition of the player

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
